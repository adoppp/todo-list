import { StatusFilter } from "components/StatusFilter/StatusFilter"

export const AppBar = () => {
    return (
        <header>
            <div>
                <div></div>
                <StatusFilter />
            </div>
        </header>
    )
}